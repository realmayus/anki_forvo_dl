{
  "noteTypeSpecific": {
    "searchField": {
      "friendly": "Search Field",
      "default": "Word",
      "description": "<p>Select the field whose text should be used to search on Forvo.</p><p>Note type: <b>{{noteType}}</b></p>",
      "type": "text"
    },
    "audioField": {
      "friendly": "Audio Field",
      "description": "<p>Select the field where the audio should be put in.</p><p>Note type: <b>{{noteType}}</b></p>",
      "type": "text"
    }
  },
  "deckSpecific": {
    "language": {
      "friendly": "Deck Language",
      "description": "The language of this deck that will be used to search on Forvo. [See all possible options](https://github.com/realmayus/anki_forvo_dl/blob/main/assets/languages.json)",
      "default": "ja",
      "type": "lang"
    }
  },
  "audioFieldAddMode": {
    "friendly": "Audio field add mode",
    "description": "Allows you to specify how the audio string should be added to the audio field. You can choose between 'prepend', 'replace' and 'append'.",
    "default": "append",
    "type": "choice",
    "options": [
      "prepend", "replace", "append"
    ]
  },
  "skipExistingBulkAdd": {
    "friendly": "Skip existing",
    "description": "Skip cards that already have content in the audio field (Bulk add mode)",
    "default": true,
    "type": "boolean"
  },
  "playAudioAfterSingleAddAutomaticSelection": {
    "friendly": "Play audio after automatic selection (single add)",
    "description": "Play the audio that was selected automatically when the shift key was held down when pressing the blue forvo button in the editor",
    "default": true,
    "type": "boolean"
  },
  "replaceCharacters": {
    "friendly": "Characters to replace",
    "description": "A list of characters that should be removed from the search term before searching on forvo.",
    "default": [
        "・", "~", "。", "."
    ],
    "type": "stringlist"
  }
}